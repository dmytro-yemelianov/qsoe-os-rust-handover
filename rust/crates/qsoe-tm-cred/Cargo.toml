[package]
name = "qsoe-tm-cred"
version = "0.0.0"
edition.workspace = true
license.workspace = true
publish.workspace = true

[lib]
name = "qsoe_tm_cred"
path = "src/lib.rs"
crate-type = ["rlib", "staticlib"]

[features]
host-tests = []
