# Rust Migration Status

Captured: 2026-07-01 CEST.

This table tracks components whose current C implementation may be replaced by
Rust. Link-smoke binaries, examples, and reusable parser crates are not listed
unless they replace a C component in an image or test path.

State meanings:

- `C default`: normal builds and images use the C implementation.
- `Rust opt-in`: a narrow build flag or smoke path can select Rust while C
  remains the rollback path.
- `Rust default`: release-candidate or normal images select Rust by default.
- `Retired`: the C implementation is removed from normal source or image paths.

| Component | C default | Rust opt-in | Rust default | Retired | Evidence / selector | Next gate |
| --- | --- | --- | --- | --- | --- | --- |
| Host qrvfs tools | No: `host_tools/treeqrvfs.c` and `host_tools/mkfs-qrv.c` are removed. | Historical only: Rust `qrvfs-tree` and `mkfs-qrv-rs` passed opt-in and RC fixture/live-image evidence before retirement. `QSOE_RUST_TREEQRVFS=0` and `QSOE_RUST_MKFS_QRV=0` are now rejected. | Yes: `make tree`, `make treeqrvfs-rc-smoke`, `make fsqrv-image`, `make rust-mkfs-qrv-live-smoke`, and `make mkfs-qrv-rc-live-smoke` use Rust only. | Yes | qrvfs behavior spec, Rust parser tests, Rust fixture checks, selected artifact smoke, Rust writer fixture, production-root writer comparison, stale-target initialization test, bounded triple-indirect allocator test, Rust-written live virtio `/usr` smoke, historical `TREEQRVFS_RC.md` / `MKFS_QRV_RC.md`, and `HOST_QRVFS_RETIREMENT.md`. | Keep Rust-only host qrvfs tool paths green; no C rollback target remains. |
| `slogger` | No: tracked `quser` source removes the C `/sbin/slogger` service, and NQ/LQ image packaging now supplies Rust `slogger-rs` through `SBIN_SLOG_ELF`. | Historical only: `QSOE_RUST_SLOGGER=1` became the default before retirement. `QSOE_RUST_SLOGGER=0` is now rejected. | Yes: `make slogger-artifact` stages Rust `slogger-rs` as `build/rust/selected/sbin/slogger.elf`; NQ/LQ CPIO recipes stage it at `/sbin/slogger`; `make slogger-rc-readback-smoke` validates the Rust-only image path. | Yes | Behavior spec, C and Rust-selected `/dev/slog` readback smokes, Rust ring tests, link smoke, ELF audit, boot-log comparison, historical `SLOGGER_RC.md`, accepted #95 local-equivalent RC evidence, and `SLOGGER_RETIREMENT.md`. | Keep Rust-only service path; no C rollback target remains. |
| `devb-virtio` | No: tracked `quser` source removes the C `/sbin/devb-virtio` block driver, and NQ/LQ image packaging now supplies Rust `devb-virtio-rs` through `SBIN_VIRTIO_ELF`. | Historical only: `QSOE_RUST_VIRTIO=1` became the default before retirement. `QSOE_RUST_VIRTIO=0` is now rejected. | Yes: `make virtio-artifact` stages Rust `devb-virtio-rs` as `build/rust/selected/sbin/devb-virtio.elf`; NQ/LQ CPIO recipes stage it at `/sbin/devb-virtio`; `make virtio-rc-file-smoke` validates the Rust-only file-read path. | Yes | Behavior spec, MMIO and queue tests, link smoke, ELF audit, Rust-selected boot and file-read smokes, historical `VIRTIO_RC.md`, and `VIRTIO_RETIREMENT.md`. | Keep Rust-only block-driver path; no C rollback target remains. |
| `pipe` | No: tracked `quser` source removes the C `/sbin/pipe` service, and NQ/LQ image packaging now supplies Rust `pipe-rs` through `SBIN_PIPE_ELF`. | Historical only: `QSOE_RUST_PIPE=1` became the default before retirement. `QSOE_RUST_PIPE=0` is now rejected. | Yes: `make pipe-artifact` stages Rust `pipe-rs` as `build/rust/selected/sbin/pipe.elf`; NQ/LQ CPIO recipes stage it at `/sbin/pipe`; `make pipe-rc-data-smoke` validates the Rust-only image path. | Yes | C mini-spec, C smoke, `qsoe-pipe` host tests, selector, link smoke, ELF audit, Rust-selected registration boot smoke, Rust-selected pipe(2) data-path smoke, trusted `main` CI run `28102250069` for #96, historical `PIPE_RC.md`, and `PIPE_RETIREMENT.md`. | Keep Rust-only service path; no C rollback target remains. |
| `test_msgpass` | No: the C helper under `quser/test/msgpass` is removed by the tracked component override and is no longer staged into test images. | Historical only: the opt-in selector became the RC path before retirement. `QSOE_RUST_TEST_MSGPASS=0` is now rejected. | Yes: `make test-msgpass-artifact` always stages Rust `test_msgpass-rs` as `/usr/bin/test_msgpass`; `make rust-test-msgpass-smoke` and `make test-msgpass-rc-smoke` validate the image path. | Yes | Helper contract, runtime-buffer Rust helper, selector, link smoke, Rust-selected suite `[msgpass]` smoke, trusted `main` CI run `28102250069` for #97, historical `TEST_MSGPASS_RC.md`, and `TEST_MSGPASS_RETIREMENT.md`. | Keep Rust-only image path; use this removal as the first #26 retirement checklist exercise before attempting production-service retirement. |
| `tm_procfs` | No: `libtaskman/src/tm_procfs.c` is removed; `libtaskman/include/tm_procfs.h` remains as the C ABI consumed by taskman glue. | Historical only: `QSOE_RUST_TM_PROCFS=1` became the default before retirement. `QSOE_RUST_TM_PROCFS=0` is now rejected. | Yes: Rust `qsoe-tm-procfs` is mandatory in the shared `qsoe-tm-providers` archive; `make tm-procfs-rc-smoke` validates the Rust-only `/proc` path. | Yes | Task-manager inventory, C/Rust boundary review, Rust host tests, selected NQ/LQ taskman links, soft-float Rust archive audit, retired selector rejection, `make tm-procfs-evidence`, `TASK_MANAGER_PROCFS_RC.md`, `TASK_MANAGER_PROCFS_RETIREMENT.md`, shared multi-provider evidence through `make tm-providers-evidence`, and trusted `main` CI run `28395213513` for #179. | Keep Rust-only taskman provider path; no C rollback target remains. |
| `tm_cpio` | No: `libtaskman/src/cpio.c` is removed; `libtaskman/include/tm_cpio.h` remains as the C ABI consumed by taskman glue and path-manager code. | Historical only: `QSOE_RUST_TM_CPIO=1` became the default before retirement. `QSOE_RUST_TM_CPIO=0` is now rejected. | Yes: Rust `qsoe-tm-cpio` is mandatory in the shared `qsoe-tm-providers` archive; `make tm-cpio-rc-smoke` validates the Rust-only CPIO symlink/file-read/spawn path. | Yes | Rust host tests, selected NQ/LQ taskman links with no `cpio.o`, soft-float Rust archive audit, retired selector rejection, exported symbol audit, LQ CPIO symlink/file-read/spawn runtime smoke, `TASK_MANAGER_CPIO.md`, `TASK_MANAGER_CPIO_RC.md`, and `TASK_MANAGER_CPIO_RETIREMENT.md`. | Keep Rust-only taskman provider path; no C rollback target remains. |
| `tm_cred` | No: `libtaskman/src/cred.c` is removed; `libtaskman/include/tm_cred.h` remains as the C ABI consumed by taskman process code. | Historical only: `QSOE_RUST_TM_CRED=1` became the default before retirement. `QSOE_RUST_TM_CRED=0` is now rejected. | Yes: Rust `qsoe-tm-cred` is mandatory in the shared `qsoe-tm-providers` archive; `make tm-cred-rc-smoke` validates the Rust-only credential policy path. | Yes | Rust host tests, selected NQ/LQ taskman links with no `cred.o`, soft-float Rust archive audit, retired selector rejection, exported symbol audit, LQ uid/gid/cwd/umask/permission/spawn-inheritance runtime smoke, `TASK_MANAGER_CRED.md`, `TASK_MANAGER_CRED_RC.md`, and `TASK_MANAGER_CRED_RETIREMENT.md`. | Keep Rust-only taskman provider path; no C rollback target remains. |
| `tm_elf` | No: `libtaskman/src/elf.c` is removed; `libtaskman/include/tm_elf.h` remains as the C ABI consumed by taskman spawn and relocation glue. | Historical only: `QSOE_RUST_TM_ELF=1` became the default before retirement. `QSOE_RUST_TM_ELF=0` is now rejected. | Yes: Rust `qsoe-tm-elf` is mandatory in the shared `qsoe-tm-providers` archive; `make tm-elf-rc-smoke` validates the Rust-only dynamic ELF spawn path. | Yes | Rust host tests, selected NQ/LQ taskman links with no `elf.o`, soft-float Rust archive audit, retired selector rejection, exported symbol audit, LQ dynamic ELF spawn runtime smoke, `TASK_MANAGER_ELF.md`, `TASK_MANAGER_ELF_RC.md`, and `TASK_MANAGER_ELF_RETIREMENT.md`. | Keep Rust-only taskman provider path; no C rollback target remains. |
| `tm_reloc` | No: `libtaskman/src/reloc.c` is removed; `libtaskman/include/tm_reloc.h` remains as the C ABI consumed by taskman spawn and relocation glue. | Historical only: `QSOE_RUST_TM_RELOC=1` became the default before retirement. `QSOE_RUST_TM_RELOC=0` is now rejected. | Yes: Rust `qsoe-tm-reloc` is mandatory in the shared `qsoe-tm-providers` archive; `make tm-reloc-provider-evidence` validates the Rust-only relocation provider path. | Yes | Historical C relocation evidence, Rust host parity tests, soft-float provider archive audit, retired selector rejection, LQ taskman link with no `reloc.o`, exported symbol audit, LQ libc.so/rtld/main relocation runtime logs, `TM_RELOC_RUST_PROVIDER_PLAN.md`, and `TASK_MANAGER_RELOC_RETIREMENT.md`. | Keep Rust-only relocation provider path; broader spawn/capability/loader ownership remains C-owned and deferred. |
| `tm_fdt` | No: `lq/taskman/sys/fdt.c` is removed by the tracked component override; `lq/taskman/sys/fdt.h` remains as the C ABI consumed by LQ taskman boot glue. | Historical only: `QSOE_RUST_TM_FDT=1` became the default before retirement. `QSOE_RUST_TM_FDT=0` and `TM_FDT_RC_ROLLBACK=1` are now rejected. | Yes: Rust `qsoe-tm-fdt` is mandatory in the shared `qsoe-tm-providers` archive; `make tm-fdt-rc-smoke` validates the Rust-only LQ runtime path. | Yes | Rust host tests, LQ Rust-only taskman link with no `sys/fdt.o`, soft-float Rust archive audit, exported symbol audit, retired selector rejection, final taskman ELF audit, LQ `/chosen` bootargs, syscfg/sysmap, `/sys`, and `sysinfo` runtime smoke, `TASK_MANAGER_FDT.md`, `TASK_MANAGER_FDT_RC.md`, and `TASK_MANAGER_FDT_RETIREMENT.md`. | Keep Rust-only FDT provider path; no C rollback target remains. |
| `tm_pathmgr` | No: `libtaskman/src/pathmgr.c` is removed; `libtaskman/include/tm_pathmgr.h` remains as the C ABI consumed by taskman path and process code. | Historical only: `QSOE_RUST_TM_PATHMGR=1` became the default before retirement. `QSOE_RUST_TM_PATHMGR=0` is now rejected. | Yes: Rust `qsoe-tm-pathmgr` is mandatory in the shared `qsoe-tm-providers` archive; `make tm-pathmgr-rc-smoke` validates the Rust-only path registry. | Yes | Rust host tests, selected NQ/LQ taskman links with no `pathmgr.o`, soft-float Rust archive audit, retired selector rejection, exported symbol audit, LQ `/dev` readdir, `/etc` symlink, repath, helper registration/duplicate rejection/MsgSend/unregister runtime smoke, `TASK_MANAGER_PATHMGR.md`, `TASK_MANAGER_PATHMGR_RC.md`, and `TASK_MANAGER_PATHMGR_RETIREMENT.md`. | Keep Rust-only taskman provider path; no C rollback target remains. |
| `tm_pseudodev` | No: `lq/taskman/sys/devnull.c` and `lq/taskman/sys/devzero.c` are removed by the tracked component override; their headers remain as the C ABI consumed by LQ taskman path IO. | Historical only: `QSOE_RUST_TM_PSEUDODEV=1` became the default before retirement. `QSOE_RUST_TM_PSEUDODEV=0` is now rejected. | Yes: Rust `qsoe-tm-pseudodev` is mandatory in the shared `qsoe-tm-providers` archive; `make tm-pseudodev-rc-smoke` validates the Rust-only pseudo-device path. | Yes | Rust host tests, LQ taskman links with no `devnull.o` or `devzero.o`, soft-float Rust archive audit, retired selector rejection, exported symbol audit, live `/dev/null` and `/dev/zero` open/write/read/fstat runtime smoke, `TASK_MANAGER_PSEUDODEV.md`, `TASK_MANAGER_PSEUDODEV_RC.md`, and `TASK_MANAGER_PSEUDODEV_RETIREMENT.md`. | Keep Rust-only taskman provider path; no C rollback target remains. |
| `tm_rsrcdb` | No: `lq/taskman/sys/rsrcdb.c` is removed by the tracked component override; `lq/taskman/sys/rsrcdb.h` remains as the C ABI consumed by LQ taskman IPC dispatch. | Historical only: `QSOE_RUST_TM_RSRCDB=1` became the default before retirement. `QSOE_RUST_TM_RSRCDB=0` is now rejected. | Yes: Rust `qsoe-tm-rsrcdb` is mandatory in the shared `qsoe-tm-providers` archive; `make tm-rsrcdb-rc-smoke` validates the Rust-only resource DB path. | Yes | Rust host tests, LQ taskman links with no `sys/rsrcdb.o`, soft-float Rust archive audit, retired selector rejection, exported symbol audit, live `rsrcdbmgr_*` create/attach/query/detach/destroy runtime smoke, `TASK_MANAGER_RSRCDB.md`, `TASK_MANAGER_RSRCDB_RC.md`, and `TASK_MANAGER_RSRCDB_RETIREMENT.md`. | Keep Rust-only taskman provider path; no C rollback target remains. |
| `tm_script` | No: `libtaskman/src/script.c` is removed; `libtaskman/include/tm_script.h` remains as the C ABI consumed by taskman spawn code. | Historical only: `QSOE_RUST_TM_SCRIPT=1` became the default before retirement. `QSOE_RUST_TM_SCRIPT=0` is now rejected. | Yes: Rust `qsoe-tm-script` is mandatory in the shared `qsoe-tm-providers` archive; `make tm-script-rc-smoke` validates the Rust-only direct shebang-spawn path. | Yes | Rust host tests, selected NQ/LQ taskman links with no `script.o`, soft-float Rust archive audit, retired selector rejection, exported symbol audit, LQ direct shebang-spawn runtime smoke, `TASK_MANAGER_SCRIPT.md`, `TASK_MANAGER_SCRIPT_RC.md`, and `TASK_MANAGER_SCRIPT_RETIREMENT.md`. | Keep Rust-only taskman provider path; no C rollback target remains. |
| `tm_syscfg` | No: `libtaskman/src/syscfg.c` is removed; `libtaskman/include/tm_syscfg.h` remains as the C ABI consumed by taskman glue. LQ's private `lq/taskman/sys/syscfg.c` remains C by design. | Historical only: `QSOE_RUST_TM_SYSCFG=1` became the default before retirement. `QSOE_RUST_TM_SYSCFG=0` is now rejected. | Yes: Rust `qsoe-tm-syscfg` is mandatory in the shared `qsoe-tm-providers` archive; `make tm-syscfg-rc-smoke` validates the Rust-only `/sys` and `sysinfo` consumer path. | Yes | Rust host tests, selected NQ/LQ `libtaskman.a` links with no `syscfg.o`, soft-float Rust archive audit, retired selector rejection, exported symbol audit in the provider archive, LQ `/sys` and `sysinfo` runtime smoke, `TASK_MANAGER_SYSCFG.md`, `TASK_MANAGER_SYSCFG_RC.md`, and `TASK_MANAGER_SYSCFG_RETIREMENT.md`. | Keep Rust-only taskman provider path; no C rollback target remains. |
| `tm_sysmap` | No: `lq/taskman/sys/sysmap.c` is removed by the tracked component override; `lq/taskman/sys/sysmap.h` remains as the C ABI consumed by taskman spawn and main glue. | Historical only: `QSOE_RUST_TM_SYSMAP=1` became the default before retirement. `QSOE_RUST_TM_SYSMAP=0` is now rejected. | Yes: Rust `qsoe-tm-sysmap` is mandatory in the shared `qsoe-tm-providers` archive; `make tm-sysmap-rc-smoke` validates the Rust-only spawned-child `PSYS` page path. | Yes | Rust host tests, LQ taskman link with no C `sys/sysmap.o`, soft-float Rust archive audit, retired selector rejection, exported symbol audit, spawned-child `PSYS` page runtime smoke through `sysinfo` timebase/PLIC/PCI output, `TASK_MANAGER_SYSMAP.md`, `TASK_MANAGER_SYSMAP_RC.md`, and `TASK_MANAGER_SYSMAP_RETIREMENT.md`. | Keep Rust-only LQ sysmap provider path; no C rollback target remains. |
| `tm_sysfs` | No: `libtaskman/src/tm_sysfs.c` is removed; `libtaskman/include/tm_sysfs.h` remains as the C ABI consumed by taskman path glue. | Historical only: `QSOE_RUST_TM_SYSFS=1` became the default before retirement. `QSOE_RUST_TM_SYSFS=0` is now rejected. | Yes: Rust `qsoe-tm-sysfs` is mandatory in the shared `qsoe-tm-providers` archive; `make tm-sysfs-rc-smoke` validates the Rust-only `/sys` path. | Yes | Rust host tests, selected NQ/LQ `libtaskman.a` links with no `tm_sysfs.o`, soft-float Rust archive audit, retired selector rejection, exported symbol audit in the provider archive, LQ `/sys` readdir and all-file runtime smoke, `TASK_MANAGER_SYSFS.md`, `TASK_MANAGER_SYSFS_RC.md`, and `TASK_MANAGER_SYSFS_RETIREMENT.md`. | Keep Rust-only taskman provider path; no C rollback target remains. |

Host `treeqrvfs` and host `mkfs-qrv` are retired-C Rust-only host tools. `test_msgpass`,
`slogger`, `pipe`, `devb-virtio`, `tm_procfs`, `tm_cpio`, `tm_script`,
`tm_cred`, `tm_elf`, `tm_reloc`, `tm_syscfg`, `tm_sysmap`, `tm_sysfs`, `tm_pathmgr`,
`tm_pseudodev`, `tm_rsrcdb`, and `tm_fdt` have moved past RC into `Retired`
status and are Rust-only in their host-tool, image, or taskman paths.
Selected taskman Rust providers now link through one shared
`qsoe-tm-providers` archive when any `QSOE_RUST_TM_*` selector is enabled, so
multi-provider evidence can be gathered without duplicate panic-handler
symbols. C remains the rollback path only for non-retired current migration candidates.
