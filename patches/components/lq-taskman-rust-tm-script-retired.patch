diff --git a/taskman/Makefile b/taskman/Makefile
--- a/taskman/Makefile
+++ b/taskman/Makefile
@@ -59,6 +59,9 @@ TM_RUST_PROVIDER_COUNT := $(words $(filter 1,$(QSOE_RUST_TM_CPIO) $(QSOE_RUST_T
 ifneq ($(QSOE_RUST_TM_PROCFS),1)
 $(error QSOE_RUST_TM_PROCFS must be 1 after C tm_procfs retirement)
 endif
+ifneq ($(QSOE_RUST_TM_SCRIPT),1)
+$(error QSOE_RUST_TM_SCRIPT must be 1 after C tm_script retirement)
+endif
 
 TASKMAN_DIR := $(TOP)/taskman
 # Public QSOE/libc headers (sys/qsoe.h, qsoe/tm_msgs.h, ...) live in
